# Jackpot Monsters Prototype

Static frontend for Card Combat. Deployed via GitHub Pages.
Backend WebSocket can be set via localStorage key 'cc_ws_url' (e.g., wss://<your-render-service>.onrender.com).
